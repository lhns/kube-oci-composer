# A stand-in for Flux's GitRepository, shared by the e2e cluster and the envtest suites.
#
# Installing Flux to test this controller would test Flux. What both suites need is the CONTRACT:
# an object with a status.artifact carrying url, digest and revision. This declares exactly that
# and nothing else.
#
# Lives here rather than under test/e2e/manifests because envtest loads CRDs by DIRECTORY, so a
# directory holding only CRDs is what makes it usable from both.
# A minimal stand-in for source-controller's GitRepository.
#
# The e2e cluster does not run Flux, and installing it would test Flux rather than this controller.
# What ImageBuild actually consumes is three fields of the contract — status.artifact.url,
# .digest and .revision — so this declares exactly those and nothing else.
#
# Deliberately NO status subresource: without one, `kubectl apply` can set status directly, which
# is what lets the harness publish an artifact without a controller to do it. A real Flux install
# has the subresource and source-controller writes it.
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: gitrepositories.source.toolkit.fluxcd.io
spec:
  group: source.toolkit.fluxcd.io
  names:
    kind: GitRepository
    listKind: GitRepositoryList
    plural: gitrepositories
    singular: gitrepository
  scope: Namespaced
  versions:
    - name: v1
      served: true
      storage: true
      schema:
        openAPIV3Schema:
          type: object
          properties:
            spec:
              type: object
              x-kubernetes-preserve-unknown-fields: true
            status:
              type: object
              properties:
                artifact:
                  type: object
                  properties:
                    url:
                      type: string
                    digest:
                      type: string
                    revision:
                      type: string
